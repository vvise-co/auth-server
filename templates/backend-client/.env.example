# =================================
# Backend Client Environment
# =================================

# Server Configuration
SERVER_PORT=8080
APP_NAME=your-project-name

# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/your_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Auth Server Configuration
# URL of the central auth server (the demo project)
AUTH_SERVER_URL=http://localhost:8081

# JWT Secret - MUST match the auth server's JWT_SECRET
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-must-be-at-least-256-bits-long-for-security

# Access token expiration in milliseconds (should match auth server)
ACCESS_TOKEN_EXPIRATION=900000

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
